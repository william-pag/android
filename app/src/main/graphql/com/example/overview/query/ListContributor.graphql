query ListContributor($strategyId: Float, $departmentIds: [Float!]) {
    listContributors(strategyId: $strategyId, departmentIds: $departmentIds) {
        overall
        complete
        percentComplete
    }
}