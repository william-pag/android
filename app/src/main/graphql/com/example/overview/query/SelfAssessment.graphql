query SelfAssessment($strategyId: Float, $departmentIds: [Float!]) {
    selfAssessments(strategyId: $strategyId, departmentIds: $departmentIds) {
        overall
        complete
        percentComplete
    }
}